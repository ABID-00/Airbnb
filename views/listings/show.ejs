<% layout("/layouts/boilerplate") -%>

<!-- <style>
  #bnb {
    width: 200px;
    height: 200px;
    border-radius: 10px;
    margin: 10px;
  }
  a {
    text-decoration: none;
  }
</style> -->

<body>
  <div class="container">
    <div class="row">
      <div class="col-md-8 offset-md-2 text-center">
        <h3>About <i><%= list.title %></i></h3>
        <img src="<%= list.image.url %>" alt="bnb image" class="img-fluid rounded mb-3" style="max-height: 300px; object-fit: cover;" />
        <div class="card-text mb-3">
          <p><%= list.title %></p>
          <p><%= list.description %></p>
          <p>₹<%= list.price.toLocaleString("en-IN")%></p>
          <p><%= list.country %></p>
        </div>
        <div class="d-flex justify-content-center gap-3">
          <form action="/listings/<%=list._id%>/edit">
            <button class="btn btn-primary">Edit</button>
          </form>
          <form action="/listing/<%=list._id%>?_method=DELETE" method="POST">
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
